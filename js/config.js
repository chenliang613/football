// ============================================================
// config.js — 数据源配置
// ============================================================
//
// 如何启用实时数据（三步）：
//   1. 免费注册  →  https://www.football-data.org/client/register
//   2. 注册后在邮件里获取 API Token
//   3. 将 Token 填入下方 API_KEY，保存文件
//      → 然后点击页面右下角 📡 → "立即刷新" 即可拉取实时数据
//
// 实时数据会自动更新以下三个接口：
//   积分榜  GET {DATA_URL}/competitions/PL/standings?season=2025
//   赛程    GET {DATA_URL}/competitions/PL/matches?season=2025
//   射手榜  GET {DATA_URL}/competitions/PL/scorers?season=2025&limit=20
//
// 若不填 API_KEY，应用将使用内置静态数据（离线模式），不影响正常浏览。
// ============================================================

const CONFIG = {

  // ──────────────────────────────────────────────────────────
  // 🔑 API Key — 填入后点击刷新按钮即可获取实时数据
  //    留空 = 离线模式（使用内置静态数据）
  // ──────────────────────────────────────────────────────────
  API_KEY: 'abc1234def5678gh90ij',

  // ──────────────────────────────────────────────────────────
  // 📡 数据源地址
  //    默认使用 football-data.org 官方 API
  //    如需通过代理或切换数据源，修改此字段即可
  // ──────────────────────────────────────────────────────────
  DATA_URL:       'https://api.football-data.org/v4',
  COMPETITION_ID: 'PL',    // 联赛代码：PL = 英超，BL1 = 德甲，PD = 西甲，SA = 意甲，FL1 = 法甲
  SEASON:         2025,    // 赛季年份（2025 = 2025/26 赛季）

  // ──────────────────────────────────────────────────────────
  // ⏱ 刷新策略
  // ──────────────────────────────────────────────────────────
  // 自动刷新间隔（毫秒）：
  //   比赛进行中建议  60_000（每1分钟）
  //   普通浏览建议   300_000（每5分钟）
  REFRESH_INTERVAL: 5 * 60 * 1000,

  // 内存缓存有效期（毫秒）：防止短时间内重复请求消耗配额
  CACHE_TTL: 3 * 60 * 1000,
};
